RewriteEngine On
RewriteBase /edm-task-monitoring/backend/

# Serve existing files/directories as-is (spa assets, etc.)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [L]
